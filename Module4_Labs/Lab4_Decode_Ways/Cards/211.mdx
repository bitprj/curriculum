<!--Title={Alphabet or Numbers}-->

<!--concepts={}-->

# Creating a datastructure for your result
Use `len()` to determine how many zeroes are needed to fill the list.
Then you can hard code the first element to be 1
```python
dp = [0] * (len(s) + 1)
dp[0] = 1
```
