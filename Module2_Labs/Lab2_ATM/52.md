<!--title={Login and looping the menu}-->

<!--concepts={IfStatements.mdx, WhileLoops.mdx, BooleanOperators.mdx, PrintStatements.mdx, Variables.mdx}-->

<!--badges={Python:4,Software Engineering:1}-->

We need to use `login()` to log the user in before we begin printing the menu, after the `login()` succeeds.

The menu is always printed out repeatedly. We can use a **while loop** after logging the user in. We also need to check the success of the login process with an **if statement**.

