<!--title={CPU moves}-->

<!--badges={Python:22,Software Engineering:7,Creative Thinker:4}-->

<!--concepts={}-->

We can use the `choice()` function from the imported **package** `random`. This will select a random element from a list. The cpu can only choose a free square, which we can use `freeSpaces` for. We also need to update `boardData` as appropriate.
