<!--title={If Operations}-->

<!--badges={Python:8,Software Engineering:2}-->

<!--concepts={Functions.mdx, PrintStatements.mdx, Variables.mdx, IfStatements.mdx, BooleanOperators.mdx}-->

Create if and else-if (`elif`) statements for each option on the menu.

Then, using the add, subtract, divide, and multiply functions you created earlier, calculate each outcome with the user inputted numbers.

Once the two numbers are calculated, print out the equation and the final answer using the `print` function.

If the user chooses a value that is not an option on the menu, have the console print "Invalid input".

